export enum LS {
  LS_TOKEN_SYSTEM = 'token-we-chess-web',
  LS_LANGUAGE = 'language-we-chess-web',
}

export enum LimitValues {
  TEN = 10,
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}
